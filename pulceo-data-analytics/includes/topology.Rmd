---
params:
  rootfolder: "."
---

```{r setup-topology, include=FALSE}
knitr::opts_knit$set(root.dir = here())
```

```{r topology-directories, include=FALSE, echo=FALSE}
source(here("includes/R/directories.R"))
source(here("includes/R/tables.R"))
```

```{r topology-data, include=FALSE, echo=FALSE}
source(here("includes/resources/providers.R"))
source(here("includes/resources/nodes.R"))
source(here("includes/resources/links.R"))
source(here("includes/resources/metric-requests.R"))
source(here("includes/resources/applications.R"))
```

## Topology

```{r topology-providers, eval=nrow(providers) > 0, child = 'includes/topology/providers.Rmd', include=TRUE, echo=FALSE}
```

```{r topology-nodes, eval=nrow(nodes) > 0, child = 'includes/topology/nodes.Rmd', include=TRUE, echo=FALSE}
```

```{r topology-links, eval=nrow(links) > 0, child = 'includes/topology/links.Rmd', include=TRUE, echo=FALSE}
```

```{r topology-metric-requests, eval=nrow(metric_requests) > 0, child = 'includes/topology/metric-requests.Rmd', include=TRUE, echo=FALSE}
```

```{r topology-applications, eval=nrow(applications) > 0, child = 'includes/topology/applications.Rmd', include=TRUE, echo=FALSE}
```
